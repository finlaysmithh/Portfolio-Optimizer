from __future__ import annotations

"""
Forecasting package: per-ticker return forecasters and covariance projection.

This package is import-safe: heavy/optional deps (e.g., pmdarima, arch)
are only imported inside functions and guarded with try/except.
"""

